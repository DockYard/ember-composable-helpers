import { filter } from 'dummy/helpers/filter';
import { module, test } from 'qunit';

module('Unit | Helper | filter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filter([42]);
  assert.ok(result);
});
